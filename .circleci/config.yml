orbs:
  node: circleci/node@4.2.1

version: 2.1

jobs:
   build:
      docker:
        - image: docker
      steps:
        - run: echo "Building"
        - run: sleep 10
        - run: echo "Build Succeed"

workflows:
  app-tests:
    jobs:
      - build